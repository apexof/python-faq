#### Как устроена память в питоне?

**1. Все - объекты**
Переменные хранят ссылки на объекты, а не сами значения.
Объект содержит: значение, тип, счётчик ссылок.
`a = b` не копирует объект, а добавляет ещё одну ссылку на тот же объект.

```python
a = [1, 2, 3]  # a ссылается на список
b = a          # b ссылается на тот же список
```

**2. Reference Counting**
Каждый объект имеет счётчик ссылок `ob_refcnt`.
Когда счётчик = 0, память освобождается.

```python
import sys
sys.getrefcount(a)  # показывает количество ссылок
```

**3. Garbage Collector**
Дополнительно к reference counting.
Работает периодически или при превышении порогов.

#### Что такое garbage collector?
Garbage collector (GC) — автоматическая система удаления объектов, на которые больше никто не ссылается. Находит и удаляет циклические ссылки (A → B → A).

#### Что такое циклические ссылки?
Объекты, которые ссылаются друг на друга:

```python
a = []
b = []
a.append(b)
b.append(a)
```

Они "держат" друг друга, хотя больше никому не нужны. Python умеет находить такие циклы и удалять.

#### В чем разница между == и is?
`==` - Вызывает метод `__eq__`.
У встроенных типов он сравнивает по значению.
Сначала вызывает `a.__eq__(b)`, если его нет то `b.__eq__(a)`.
Если метод `__eq__` не реализован он берется из `object` так как все классы от него наследуются.
В `object` метод `__eq__` реализован как a is b.

`is` - проверяет, ссылаются ли две переменные на один и тот же объект.
По сути эквивалентно `id(x) == id(y)`, но на практике `is` работает напрямую и быстрее.

Проверяет являются ли две переменные одним и тем же объектом в памяти.

```python
# Списки с одинаковым содержимым
a = [1, 2, 3]
b = [1, 2, 3]

a == b  # True - значения равны
a is b  # False - разные объекты в памяти

# Одна ссылка
c = a
c is a  # True - один объект
c == a  # True - значения тоже равны
```

#### Почему `a is b` для 256, но не для 257?
```python
a = 256
b = 256
a is b   # True  — один закэшированный объект

a = 257
b = 257
a is b   # False — два разных объекта
```

Это interning (кэширование) мелких целых чисел в CPython.
При старте интерпретатор создаёт пул объектов int для диапазона от -5 до 256 включительно. Эти объекты существуют в единственном экземпляре, поэтому любая переменная с таким значением ссылается на один и тот же объект.

#### Кэшируются ли строки так же?
Да, есть string interning, но правила другие: интернируются строки, похожие на идентификаторы (буквы/цифры/_). Можно форсировать через `sys.intern()`.

#### Что вернёт id()?
`id()` возвращает целое число — уникальный идентификатор объекта, постоянный на протяжении жизни этого объекта.

```python
a = []
id(a)  # напр. 140234567891234
```

Уникальность только среди живущих одновременно объектов. После уничтожения объекта его id может быть переиспользован новым объектом. Классическая ловушка:

```python
id([1,2,3]) == id([4,5,6])  # может вернуть True!
# первый список умирает сразу, второй занимает тот же адрес
```